{
  "name": "git-gud",
  "version": "0.1.0",
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "build": "npm -w packages/core run build && npm -w packages/vscode run build && npm -w packages/web run build",
    "dev": "npm -w packages/web run dev"
  }
}
